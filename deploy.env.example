# Copy to deploy.env (git-ignored) and set for your own box:
CA_BOX_HOST=you@your-ca-box.lan
# CA_SSH_KEY=~/.ssh/id_ed25519      # optional: explicit ssh -i key
# CA_BOX_SRC=/opt/nebula-ca/src     # optional: deploy target dir on the box
